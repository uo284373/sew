public static void main(String[] args) throws FileNotFoundException, DocumentException {
		generarFactura(null);
	    }
	
	public static void generarFactura(Venta venta) throws FileNotFoundException, DocumentException {
		Document doc = new Document();
		String directorio = new File("Facturas/").getAbsolutePath();
		PdfWriter pdf = PdfWriter.getInstance(doc, new FileOutputStream(directorio+"Factura_"+".pdf"));
		
		//Cliente cliente = Cliente.getCliente(venta.getIdCliente());
		
		doc.open();
		
		añadirLogo(doc);
	}

	private static void añadirLogo(Document doc) {
		// TODO Auto-generated method stub
		
	}